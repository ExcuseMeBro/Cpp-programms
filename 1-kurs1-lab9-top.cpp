#include <bits/stdc++.h>

using namespace std;

int main()
{
    double A = 2.6, B = 56.6, C = 35, D = 20.3;
    double Y = 1 / pow(A, 2.) * pow(B / 10, 3.) * pow(C + D, 2.);
    cout << "Y = " << Y << endl;
}