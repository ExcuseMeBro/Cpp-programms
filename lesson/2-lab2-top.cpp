//2-topshiriq
#include <bits/stdc++.h>

using namespace std;

int main(){
    double x;
    cout<<"x = ";
    cin>>x;
    cout<<"Natija: "<<pow(tan(x),4)+sin(M_PI/x)*sin(M_PI/x)-exp(2*x*x+3.6*x-1)<<endl;
    return 0;
}